# Firebase configuration.
# Copy this file to `.env.local` and fill in the values from your Firebase
# project settings (Project settings -> General -> Your apps -> SDK config).
# The NEXT_PUBLIC_ prefix is required so Next.js exposes these to the browser,
# where Firebase runs. These identify your project; they are not secret keys.
NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-auth-domain
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-storage-bucket
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id

# Set to "false" to enable the profile/collection saving flow (also requires
# writes to be enabled in firestore.rules). Anything else keeps the app in the
# frozen, read-only leaderboard mode used by the public deployment.
NEXT_PUBLIC_READ_ONLY=true
